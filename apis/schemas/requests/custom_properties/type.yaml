in: query
name: type
description: |
  Supported operators are `eq`, `in`.<br>
schema:
  type: string
  enum:
    - TEXT
    - NUMBER
    - SELECT
    - BOOLEAN
    - MULTI_SELECT
    - LOCATION
    - USER_LIST
    - DATE
  example: 'eq.SELECT'