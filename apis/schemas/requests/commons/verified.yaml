in: query
name: verified
schema:
  type: boolean
required: false
description: >-
  verified is used to filter expenses by verified flag.
  For example, if verified is true, it will return all the verified expenses. If verififed is false, it will return all the unverified expenses. leaving it blank will return all the expenses.